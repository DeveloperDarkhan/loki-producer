apiVersion: v1
kind: Service
metadata:
  name: loki-producer
  namespace: project-logging
  labels:
    app: loki-producer
spec:
  selector:
    app: loki-producer
  ports:
  - name: http
    port: 3101
    targetPort: 3101
  type: ClusterIP
